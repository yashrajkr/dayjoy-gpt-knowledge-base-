# Dayjoy Knowledge Base — Preliminary Missing-Knowledge Scan

Generated: 2026-08-09. **This is a preliminary scan based on what exists in the raw files today** — a full formal gap analysis (with CRITICAL/HIGH/MEDIUM/LOW priority and a `MISSING_DATA_TASKS.csv`) happens only after you approve the canonical structure and reconciliation decisions in `PROPOSED_STRUCTURE.md`, per your own workflow order. This scan exists to inform that structure proposal now.

## Confirmed present (real content, not just placeholders)

- **Product catalog**: 170 SKUs (`DJP1000`–`DJP1169`), with pricing (mrp/dp/bv/pv) verified against `India BV Price May 2026.pdf`, plus a separate 86-item catalog under different naming that needs reconciliation (see Duplicate Report).
- **FAQs**: 170 per-product FAQ files + a combined set (1,884 Q&A pairs).
- **Pricing**: May 2026 price list (CSV/JSON/SQL), BV price PDF.
- **Intent datasets**: two parallel sets (1,527 rows governance-tracked / 4,000 rows comprehensive), audience-segmented (`customer_safe_intents.csv` vs. `internal_distributor_intents.csv`), with a regulated-health-content review file already flagging medical-claim-sensitive intents.
- **RAG chunks**: two parallel sets (2,242 chunk-based / 4,000 Q&A-based).
- **Product relationships**: `dayjoy_product_relationships.csv` (~33.6KB) + a review file.
- **SQL**: schema, seed data, product/FAQ/relationship seed scripts, a Postgres-ready corpus bundle.
- **Governance scaffolding**: `canonical-answer-policy.csv`, `dayjoy-search-ranking-rules.csv`, `product-safety-classes.csv` — genuinely useful safety/answer-policy groundwork already exists.
- **QA scaffolding**: `phase9-qa-test-sheet.csv` (169KB), `dayjoy-data-qa-report-final.csv`, an AI search test set.
- **Architecture/planning documentation**: a full 32-file architecture doc set (system, database, API, deployment, security) plus a 15-file "Project_Context" set (business context, personas, tech stack, coding standards, success metrics) — this is engineering documentation for building Dayjoy AI Assist itself, not customer-facing knowledge, but valuable and complete in its own right.
- **Company/business narrative**: `Company_Research.md` (14KB), `02_Business_Model.md` (9KB), `04_Distributor_System.md` (73KB, likely contains the real compensation/binary-structure narrative).

## Gaps found (preliminary — confirm before treating as final)

### CRITICAL
- **Zero actual image files exist anywhere in the knowledge base.** Across all 1,803 file records (every ZIP fully unpacked), there is not a single `.png`/`.jpg`/`.jpeg`/`.webp`/`.gif`. Two CSV files (`dayjoy_image_metadata.csv` and `dayjoy_image_metadata_master.csv`, different row counts, unreconciled) describe images by filename/product_id, but the referenced image files themselves were never included. An AI assistant that needs to show or reference product images has nothing to point to.
- **No structured compensation plan data.** Only a 972–1,465 byte `dayjoy_compensation_plan.csv` and a 3.4KB `dayjoy_plan_ranks_rewards.csv` exist as structured data — nowhere near the depth your spec requires (joining, rank, binary structure, bonus, royalty, rewards, eligibility, promotion, commission, definitions). The real compensation detail likely lives narratively inside `04_Distributor_System.md` (73KB) and hasn't been extracted into structured, source-traceable fields. Per your rule, this must come from an official Dayjoy document — not be inferred or generated by AI.

### HIGH
- **No support/operations knowledge at all**: zero files found for refund, replacement, cancellation, shipping/delivery timelines, complaints, escalation paths, or support SLA/TAT. This entire category (Step 13 of your spec) is currently empty.
- **No training material**: zero files for beginner/intermediate/advanced onboarding, sales scripts, objection handling, follow-up, retention, presentation skills, or leadership training (Step 12). Nothing to separate into "official training" vs. "AI-generated suggestions" because nothing exists yet.
- **No golden-question set for evaluation.** Nothing matching "golden questions" was found; `dayjoy_ai_search_test_set.csv` (1KB) is small and may not be built for this purpose — needs inspection once canonical data is settled.
- **Company profile is thin as structured data.** `Dayjoy_Company_Profile.csv` and `dayjoy_company_overview.csv` are 1–2KB each — no explicit mission/vision/history/leadership/manufacturing/certifications fields found by keyword scan. The narrative likely exists in `Company_Research.md` but hasn't been structured.

### MEDIUM
- Two unreconciled image-metadata files with different row counts (5.3KB vs 9.9KB) — needs a single source of truth even once actual images are sourced.
- Two release manifests with incompatible schemas (`RELEASE_MANIFEST.csv` vs `release-manifest.csv`) — unclear which reflects the real release state.
- Gap-register lineage (826 → 1,245 → 1,360 rows) isn't confirmed as pure supersession — some previously-identified gaps may have been silently dropped rather than resolved. Worth a diff before trusting the latest count as "fewer gaps remaining."

### LOW
- Product-relationship review file (`dayjoy_product_relationships_review.csv`, 62KB) suggests open review items on relationships that were never closed out — worth checking before finalizing `02_RELATIONSHIPS/`.

## What this means for sequencing

Per your instructions, no missing data gets generated yet. This preliminary list exists only to shape `PROPOSED_STRUCTURE.md` (so the folder structure has a place for support/, training/, images/ even though they're currently empty) and to seed the eventual `MISSING_DATA_TASKS.csv` after canonical organization is approved and the Pipeline A/B reconciliation decisions (from `DUPLICATE_REPORT.md`) are made — since some "gaps" here might turn out to be already-answered once the two pipelines are merged.
