# Website Snapshots

Empty by design — see `WEBSITE_SNAPSHOT_POLICY.md` for why, and for the exact process to follow when a snapshot is captured. `website_snapshot_log.csv` is the ready-to-fill tracking template.
