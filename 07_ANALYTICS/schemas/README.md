# Analytics Schemas — not yet defined

Reserved for the data model describing how `07_ANALYTICS/metrics/` files relate to each other and to `01_CANONICAL/`. No formal analytics schema exists yet — the metrics files are standalone CSVs today. For the operational database schema (as opposed to analytics), see `05_SQL/schema/`.
